# Runtime Data

The Python API writes local JSON session files into `sessions/` and log files into `logs/`.

These files are generated while the game runs and are intentionally ignored by `.gitignore`.
